# Manager Backend API Component Specs

`component.spec.json` declares the Manager backend-api route boundary. Global HTTP, security,
Rust, SDK generation, and test rules remain authoritative in `../../../sdkwork-specs/`.
