# Manager App API Component Specs

`component.spec.json` declares the Manager app-api route boundary. Global HTTP, security, Rust,
SDK generation, and test rules remain authoritative in `../../../sdkwork-specs/`.
