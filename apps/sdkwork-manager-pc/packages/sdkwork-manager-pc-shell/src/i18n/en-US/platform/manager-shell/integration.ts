import type { ManagerIntegrationMessages } from "../../../../types/i18n";

export const managerIntegrationMessages: ManagerIntegrationMessages = {
  accessDescription: "Navigation visibility follows the IAM session permission scope. The owning backend remains the final authorization authority for every operation.",
  accessEyebrow: "Access restricted",
  accessTitle: "This operator scope cannot open the requested capability",
  description: "This host-owned route establishes the route namespace, header contract, navigation frame, and module lifecycle shared by product admin contributions.",
  displayName: "Integration",
  eyebrow: "Host capability",
  headerDescription: "Assembly, routing, entitlement metadata, and lifecycle conventions for product admin modules.",
  headerTitle: "Integration",
  lifecycleRule: "Commercial availability is declared by module metadata and enforced by the owning backend service.",
  moduleOwnershipRule: "Module packages declare their own backend SDK dependencies and operator permissions.",
  overview: "Overview",
  remoteCodeRule: "The host composes stable package exports at build time; authenticated browser sessions never load arbitrary remote code.",
  routeDescription: "Host integration contract",
  title: "Integration control plane",
};
